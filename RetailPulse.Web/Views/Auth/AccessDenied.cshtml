@{
    ViewData["Title"] = "Access Denied";
}

<div style="display: flex; flex-direction: column; align-items: center;
            justify-content: center; min-height: 60vh; text-align: center;">

    <div style="font-size: 72px; font-weight: 800; font-family: var(--font-mono);
                color: var(--red); opacity: 0.3; line-height: 1; margin-bottom: 16px;">
        403
    </div>

    <h1 style="font-size: 24px; font-weight: 700; margin-bottom: 12px;">Access Denied</h1>

    <p style="color: var(--text-faint); font-size: 14px; max-width: 400px; margin-bottom: 32px;">
        You don't have permission to perform this action. Contact your administrator if you think this is a mistake.
    </p>

    <a asp-controller="Dashboard" asp-action="Index" class="btn btn-primary">
        Back to Dashboard
    </a>
</div>